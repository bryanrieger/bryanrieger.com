---
layout: false
permalink: /feed.json
eleventyExcludeFromCollections: true
---
{
    "version": "https://jsonfeed.org/version/1",
    "home_page_url": "https://{{ site.domain }}",
    "feed_url": "https://{{ site.domain }}{{ site.feed.json }}",
    "icon": "https://{{ site.domain }}{{ site.feed.icon }}",
    "favicon": "https://{{ site.domain }}{{ site.feed.favicon }}",
    "language": "{{ site.lang }}",
    "title": "{{ site.title }}",
    "description": "{{ site.description }}",
    "authors": [
        {
          "name": "{{ site.author.name }}",
          "url": "https://{{ site.domain }}"
        }
    ],
    "items": [
      {%- for post in collections.all | reverse %}
        {
            "id": "{{ site.domain }}{{ post.url }}",
            "url": "https://{{ site.domain }}{{ post.url }}",
            "title": "{{ post.data.title }}",
            "summary": "{{ post.data.description }}",
            "date_published": "{{ post.date | dateToRfc3339 }}"
        }
        {%- endfor %}
    ]
}